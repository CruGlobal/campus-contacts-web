$("#saved_visual_tools .saved_visual_tool_line#snapshot_<%= @saved_visual_tool.id %>").remove()
if($("#saved_visual_tools .saved_visual_tool_line").size() == 0){
  $("#saved_visual_tools").text("No saved snapshots.")
}