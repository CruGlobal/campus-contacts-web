<%= check_box_tag "trend_compare", "trend_compare", @chart.trend_compare_year_ago, disabled: !@chart.trend_year_ago_available? %>
&nbsp;Compare With One Year Ago
<div class="icon help tipitright help_bubble_keyword" title="<%= t('charts.trend.year_ago') %>"></div>

<script>
  $('input#trend_compare').change(function () {
    $('div#trend_container').hide();
    $('div#trend_spinner').show();
    $.ajax({
      url: "/charts/update_trend_compare",
      type: "POST",
      data: {compare: $('input#trend_compare').prop('checked')}
    })
  });
</script>