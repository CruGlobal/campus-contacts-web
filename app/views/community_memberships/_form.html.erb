<%= form_for(@community_membership) do |f| %>
  <% if @community_membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@community_membership.errors.count, "error") %> prohibited this community_membership from being saved:</h2>

      <ul>
      <% @community_membership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <%= f.hidden_field :community_id %>
    <%= text_field_tag 'name' %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
