<div class="field <%= field.to_param %>">
  <%= label_tag field.to_param, field.label %>
  <%= form.select(:validation_method, ['', 'relay']) %>
  <% if field.has_errors? %>
  <span class="errorMessage"><%= "#{field.label } #{field.errors.first}" %></span>
  <% end %>
  <p class="help"><%= field.help %></p>
</div>