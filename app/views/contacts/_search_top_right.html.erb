<%= form_tag contacts_path, method: :get do %>
  <%= hidden_field_tag :search_type, "basic" %>
  <% if params[:include_archived].present? %>
    <%= hidden_field_tag :include_archived, true %>
  <% end %>
  <%= text_field_tag :query, params[:query], placeholder: t('people.search.search_placeholder'), autocomplete: 'off', id: 'search_autocomplete_field' %>
<% end %>
<div id="advanced_search_actions">
  <div id="advanced_search_right_container">
    <div id="search_locate_button_container">
      <% if can? :manage_locate_contact, current_organization %>
      <span><%= link_to(t('general.contact_location'), '#', class: '', id: 'locate_contact') %></span>
      <% else %>
      &nbsp;
      <% end %>
      <%= render 'shared/locate_contact' %>
    </div>
    <div id="advanced_search_button_container">
      <span id="advanced_search"><%= link_to(t('general.adv_search'), {controller: 'contacts', click: 'advanced_search_button'}, class: 'filter_link', id: 'advanced_search_button') %></span>
    </div>
  </div>
</div>