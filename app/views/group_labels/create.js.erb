<% if @group_label.present? %>
  $('#group_labels').prepend('<%= j(render('groups/label', label: @group_label)) %>')
  $.hideDialog($("#add_group_label_div"))
  $.fn.activateGroupLabelDroppable()
<% end %>
$.a("<%= @message %>")