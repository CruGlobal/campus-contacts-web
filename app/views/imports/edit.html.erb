<% provide :sidebar do %>
  <%= render 'contacts/leaders' %>
<% end %>

<%= form_for @import do |f| %>
    <% @import.headers.each_with_index do |header, i| %>
      <%= header %>: <%= select_tag "import[header_mappings][#{i}]", grouped_options_for_select(@survey_questions) %><br>
    <% end %>
  <%= submit_tag 'Update', :value=>"Update" %>
<% end %>
