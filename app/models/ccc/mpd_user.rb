class Ccc::MpdUser < ActiveRecord::Base

  def merge(other)
		#if both have mpd_user_id
			#mpd_contact.merge
			#mpd_event.merge
			#mpd_expense.merge
			#mpd_priority.merge	
			#mpd_letter.merge

		#else
    	# change old user_id to new user_id
		#end
  end
  
end
